
server.port=18100
spring.application.name=eureka-slave-server

eureka.instance.appname=eureka-slave-server
eureka.instance.instance-id=${spring.application.name}:${server.port}
eureka.instance.hostname=eureka-slave.com
eureka.instance.prefer-ip-address=false
#ע�ᵽeureka slave server
eureka.client.service-url.defaultZone=http://eureka-master.com:18050/eureka/

eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

logging.level.root: info